name: kineti-receipt
on:
  pull_request:
  push:
    branches: [main]
jobs:
  receipt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/kineti-receipt
        with:
          verify-command: "cargo build && cargo test --all"
