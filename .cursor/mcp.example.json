{
  "mcpServers": {
    "kineti": {
      "command": "bun",
      "args": [
        "${workspaceFolder}/bin/kineti-mcp.ts",
        "--workspace-root",
        "${workspaceFolder}"
      ]
    }
  }
}
